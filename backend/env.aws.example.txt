# Copy values into PowerShell ($env:VAR = "...") or your IDE run config. Do not commit secrets.
#
# DB_PASSWORD must match the RDS master password (same as CONNTO_RDS_MASTER_PASSWORD when using infra/create-rds-connto.ps1).

DB_HOST=connto-db.xxxxxxxxxxxx.region.rds.amazonaws.com
DB_PORT=5432
DB_NAME=connto
DB_USER=connto
DB_PASSWORD=change-me
DB_JDBC_PARAMS=?sslmode=require

JWT_SECRET=change-me-to-a-long-random-string

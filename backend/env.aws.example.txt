# Copy values into PowerShell ($env:VAR = "...") or your IDE run config. Do not commit secrets.
#
# DB_PASSWORD must match the RDS master password (same as CONNTO_RDS_MASTER_PASSWORD when using infra/create-rds-connto.ps1).

DB_HOST=connto-db.xxxxxxxxxxxx.region.rds.amazonaws.com
DB_PORT=5432
DB_NAME=connto
DB_USER=connto
DB_PASSWORD=change-me
DB_JDBC_PARAMS=?sslmode=require

JWT_SECRET=change-me-to-a-long-random-string

# --- ECS task definition (container environment) when using profile prod ---
# SPRING_PROFILES_ACTIVE=prod
# SPRING_DATASOURCE_URL=jdbc:postgresql://<rds-host>:5432/connto?sslmode=require
# SPRING_DATASOURCE_USERNAME=connto
# SPRING_DATASOURCE_PASSWORD=<from Secrets Manager or SSM>
# CONNTO_JWT_SECRET=<long random string>
# SERVER_SERVLET_CONTEXT_PATH=/connto
# ALB health check path: /connto/actuator/health
# See infra/README-ECS-DOCKER.txt, infra/deploy-backend-ecs.ps1, push-backend-ecr.ps1,
# ecs/fargate-task-definition.template.json, and .github/workflows/backend-ecs-deploy.yml

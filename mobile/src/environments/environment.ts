export const environment = {
  production: false,
  /** Backend base URL (no trailing slash). Use http://10.0.2.2:8080 for Android emulator → host machine. */
  apiBaseUrl: 'http://localhost:8080',
  appVersion: '0.0.1',
};

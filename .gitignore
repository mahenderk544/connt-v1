# Backend: local secrets (never commit)
backend/src/main/resources/application-local.yml
backend/target/

# Maven (if run from repo root)
**/target/

# Env files
.env
.env.local
*.env
!*.env.example

# Infra: temp trust policy JSON from create-ecs-task-execution-role.ps1 (deleted after use; ignore if left over)
infra/.ecs-trust-policy-*.json
infra/.connto-taskdef-*.json
infra/.ecs-td-*.json
infra/connto-taskdef-submit.json

# OS / IDE
.DS_Store
Thumbs.db
.idea/
**/.idea/
